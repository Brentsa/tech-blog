<section class="blog-post">
    <div class="blog-header">
        <h2>{{title}}</h2>
        <h3>Posted by {{user.username}} on {{createdAt}}</h3>
    </div>
    <div class="blog-content">
        {{content}}
    </div>
</section>
